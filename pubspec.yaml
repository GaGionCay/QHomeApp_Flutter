name: flutter_application_1
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.1.5
  http: ^1.2.0
  shared_preferences: ^2.2.2        # Đã cập nhật phiên bản mới nhất, tương thích Dart 3.9
  intl: ^0.19.0                     # Đã cập nhật phiên bản mới nhất
  fl_chart: ^0.65.0                 # Đã cập nhật phiên bản mới nhất
  flutter_secure_storage: ^9.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
